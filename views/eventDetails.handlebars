
    <div class="flex-grow">
        <div class="flex justify-center mt-6">
          <h2 class="text-2xl font-bold uppercase">{{event.name}}</h2>
        </div>
        <div class="bg-white p-6 rounded-lg shadow-lg mt-6 mx-auto w-80">
            <p class="text-gray-700"><strong>Date:</strong> {{event.date}}</p>
            <p class="text-gray-700"><strong>Time:</strong> {{event.time}}</p>
            <p class="text-gray-700"><strong>Location:</strong> {{event.location}}</p>
            <p class="text-gray-700"><strong>Description:</strong> {{event.description}}</p>
            <a href="/events/{{event.id}}/signup" class="block text-center bg-purple-600 text-white font-semibold py-2 px-4 rounded mt-4 hover:bg-purple-700 focus:outline-none focus:ring focus:ring-purple-300">Sign Up</a>
        </div>
    </div>
    <div class="flex justify-center mt-6">
          <a href="/events/view">Back to All Events</a>
    </div>
    
